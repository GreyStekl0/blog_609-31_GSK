[project]
name = "projectthree"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "django>=5.1.7",
]

[dependency-groups]
dev = [
    "ruff>=0.11.2",
]
